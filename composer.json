{
    "name": "tag1/scolta-php",
    "version": "0.2.0-dev",
    "description": "AI-powered search with Pagefind — PHP language binding",
    "type": "library",
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Tag1\\Scolta\\": "src/"
        }
    },
    "require": {
        "php": ">=8.1",
        "guzzlehttp/guzzle": "^7.0"
    },
    "suggest": {
        "extism/extism": "Required for content indexing CLI commands (build, export, rebuild-index). Not needed for serving search."
    },
    "require-dev": {
        "extism/extism": "^1.0",
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpunit/phpunit": "^10.0|^11.0"
    },
    "autoload-dev": {
        "psr-4": {
            "Tag1\\Scolta\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "lint": "php-cs-fixer fix --dry-run --diff",
        "format": "php-cs-fixer fix",
        "build-wasm": "cd ../scolta-core && cargo build --release --target wasm32-wasip1 && cp target/wasm32-wasip1/release/scolta_core.wasm ../scolta-php/wasm/scolta_core.wasm",
        "update-browser-wasm": "cp ../scolta-core/pkg/scolta_core_bg.wasm assets/wasm/ && cp ../scolta-core/pkg/scolta_core.js assets/wasm/",
        "validate-release": "php scripts/validate-release.php",
        "check-version-sync": "php scripts/check-version-sync.php"
    }
}
